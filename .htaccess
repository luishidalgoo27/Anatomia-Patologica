<IfModule mod_rewrite.c>
    RewriteEngine On

    # Evita que las rutas de la API se redirijan al directorio public
    RewriteCond %{REQUEST_URI} !^/api
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
